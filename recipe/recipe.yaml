context:
  version: "0.5.1"

package:
  name: "nvmolkit"
  version: ${{ version }}

source:
  url: https://github.com/NVIDIA-Digital-Bio/nvMolKit/archive/v${{ version }}.tar.gz
  sha256: cda91a8b499026e7595cb97a5936609a01e02dc82306401d6ad8ff96fe626f12

build:
  number: 0
  skip:
    - osx
    - win
    - cuda_compiler_version == "None"
requirements:
  build:
    - ${{ stdlib('c') }}
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ compiler('cuda') }}
    - cmake >=3.30
    - make
  host:
    - cuda-version ${{ cuda_compiler_version }}.*
    - cuda-cudart-dev
    - cuda-nvtx-dev
    - eigen
    - libboost-devel
    - libboost-headers
    - libboost-python-devel
    - libcublas-dev
    - libcurand-dev
    - libcusolver-dev
    - libgomp
    - librdkit-dev ==${{ librdkit_dev }}
    - pip
    - python
    - rdkit
    - scikit-build
    - setuptools
  run:
    - numpy
    - python
    - pytorch
    - rdkit ==${{ librdkit_dev }}
    - triton

tests:
  - python:
      imports:
        - nvmolkit
      pip_check: false

about:
  homepage: https://github.com/NVIDIA-Digital-Bio/nvMolKit
  summary: nvMolKit is a CUDA-backed python library for accelerating common RDKit molecular operations
  description: |
    nvMolKit is a CUDA-backed python library for accelerating common RDKit molecular operations.
    nvMolKit links to RDKit and nvMolKit operations work on RDKit RDMol objects.
  license_file: LICENSE/License.txt
  license: Apache-2.0

extra:
  recipe-maintainers:
    - evasnow1992
    - scal444
